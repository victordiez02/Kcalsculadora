# Usa pnpm como gestor de paquetes oficial del frontend.
# Evita que alguien instale con npm/yarn y rompa el lockfile.
package-manager-strict=true
auto-install-peers=true
strict-peer-dependencies=false
